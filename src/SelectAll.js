import React from 'react'

export const SelectAll = ({All}) => {
  //  const a= {props}
    console.log(All)
    return (
        <div>
            
            <input type="checkbox">
            </input>
            <label>Select all</label>
        </div>
    )
}
